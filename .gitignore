# compiled binary
/tfv

# generated by tfv: per-(url,ref) module cache (clones + providers)
/.tfv/

# legacy: old submodule working dir, no longer used by tfv
/.source/
/.terraform/

# OS junk
.DS_Store

# NOTE: /locks/ (committed lock files) and /.tstate/ (encrypted state) are
# intentionally NOT ignored — they belong in the repository.
